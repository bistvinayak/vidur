# Used only for local dev convenience (npm run dev). The shipped extension
# never bundles a key — end users enter their own OpenRouter key in the
# extension's Settings page, stored in chrome.storage.local on their machine.
VITE_OPENROUTER_API_KEY=
